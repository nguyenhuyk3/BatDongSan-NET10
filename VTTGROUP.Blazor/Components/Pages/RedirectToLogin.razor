@page "/"
@inject NavigationManager Navigation

<h3>Redirecting...</h3>

@code {
    protected override async Task OnInitializedAsync()
    {
        await Task.Delay(1); // đảm bảo DOM đã sẵn sàng
        Navigation.NavigateTo("/login", true);
        //Navigation.NavigateTo("/auth/login", true);
    }
}